#pragma once

namespace ofx {
namespace piMapper {

struct GuiMode {
	enum {
		NONE, TEXTURE_MAPPING, PROJECTION_MAPPING, SOURCE_SELECTION, PLAYLIST_SELECTION_MODE
	};
};

} // namespace piMapper
} // namespace ofx
